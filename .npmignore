# Exclude only legacy tmp generated schemas
lib/**/*.zod.tmp.ts

# Exclude TypeScript source files (only ship compiled JS)
lib/
index.ts
tsconfig.json

# Ensure aggregator IS published (override any ignore):
!lib/zod-schemas.ts
